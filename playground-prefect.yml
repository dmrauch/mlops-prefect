name: playground-prefect

channels:
    - conda-forge

dependencies:
    - python = 3.10

    # development dependencies
    - ipykernel
    - jupytext

    # packaging/deployment dependencies
    - pip
    - pip:
        - -r requirements.txt
